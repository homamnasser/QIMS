# مراجعة وفصل مصادقة لوحة الويب وتطبيق الموبايل

## النتيجة

أصبحت المصادقة مقسّمة إلى قناتين مستقلتين لهما عقود وبيانات اعتماد مختلفة:

| القناة | بيانات الاعتماد | مكان الحفظ | التجديد | تسجيل الخروج |
|---|---|---|---|---|
| لوحة الويب | جلسة Laravel في Cookie | Cookie سرية `HttpOnly` يديرها المتصفح | جلسة انزلاقية بحسب `SESSION_LIFETIME`، بلا Refresh Token | إبطال الجلسة وتجديد CSRF |
| تطبيق الموبايل | Access Token وRefresh Token لكل جهاز | يجب أن يحفظهما تطبيق الموبايل في مخزن أسرار نظام التشغيل | تدوير الزوج كاملًا عبر Refresh Token أحادي الاستخدام | إبطال زوج رموز الجهاز الحالي فقط |

لا تعيد استجابة دخول الويب أي Token إلى JavaScript. ولا تقبل مسارات لوحة
التحكم رموز الموبايل أو الرموز القديمة ذات الصلاحية العامة `*`. وبالعكس لا
تقبل مسارات الموبايل جلسة المتصفح.

## مشكلات المنطق السابق

### AUTH-01 — مرتفع — إعادة سر الويب في مكانين

كان الدخول المشترك ينشئ Personal Access Token غير منتهي، ثم يعيده في JSON
ويضعه أيضًا في Cookie. جعل ذلك فائدة `HttpOnly` شكلية؛ لأن JavaScript كان
قادرًا على قراءة نسخة JSON وتسريبها عند وجود XSS أو إضافة متصفح ضارة.

**المعالجة:** دخول الويب الحالي يستخدم `Auth::guard('web')` ويجدد معرّف
الجلسة، ولا ينشئ Personal Access Token ولا يعيده في الاستجابة
(`app/Http/Controllers/WebAuthenticationController.php:14-39`).

### AUTH-02 — مرتفع — تحويل Cookie إلى Bearer Token

كان Middleware خاص يقرأ Cookie ثم يحقن محتواها في ترويسة
`Authorization`. هذا يخلط نموذج جلسة المتصفح المحمي بـCSRF مع نموذج Bearer
Token، ويصعّب فرض حدود واضحة بين الويب والموبايل.

**المعالجة:** حُذف `CookieToBearer`، وفُعّل Middleware الرسمي للطلبات
الحالية من Sanctum، وصارت لوحة الويب تستخدم الجلسة وCSRF وفق النموذج
القياسي (`app/Http/Kernel.php` و`config/sanctum.php:21-41,82-86`).

### AUTH-03 — مرتفع — مسار دخول مشترك لهويات مختلفة

كان `/api/loginUser` يقبل موظفًا أو طالبًا ويعيد النوع نفسه من الرموز
للجميع. أدى ذلك إلى عقد مصادقة ملتبس، وإلى احتمال استخدام الرمز الصحيح في
القناة الخطأ.

**المعالجة:** فُصلت المسارات في
`routes/api.php:40-65`:

- `/api/web/auth/*` للموظفين في لوحة الويب.
- `/api/mobile/auth/*` للطلاب والمعلمين في تطبيق الموبايل.

ويقيّد دخول الموبايل حسابات `Student` وحسابات `User` من عائلة المعلم فقط
(`app/Services/MobileAuthenticationService.php:99-108`).

### AUTH-04 — مرتفع — رموز عامة بلا انتهاء أو تجديد منضبط

كان الرمز القديم بصلاحية `*` ومن دون `expires_at`. أي تسريب كان يبقى صالحًا
إلى أن يُحذف يدويًا، ولم يكن هناك Refresh Token أو تدوير.

**المعالجة:** يصدر الموبايل زوجًا لكل جهاز:

- Access Token بصلاحية دقيقة `mobile:access` ومدة افتراضية 60 دقيقة.
- Refresh Token بصلاحية دقيقة `mobile:refresh` ومدة افتراضية 30 يومًا.
- عند التجديد يُقفل سجل Refresh Token داخل Transaction، ثم يُحذف الزوج
  القديم ويصدر زوج جديد، ما يمنع إعادة الاستخدام والسباق المتزامن
  (`app/Services/MobileAuthenticationService.php:37-79,110-150`).
- تحذف الهجرة الانتقالية الرموز السابقة غير المبنية على زوج موبايل
  (`database/migrations/2026_07_28_000000_revoke_legacy_authentication_tokens.php:9-20`).
- تنظّف المهمة المجدولة الرموز المنتهية يوميًا
  (`app/Console/Kernel.php:13-16`).
- يضيف التحديث فهرسًا إلى `personal_access_tokens.expires_at` حتى لا يتحول
  التنظيف الدوري إلى مسح كامل للجدول مع ازدياد عدد الأجهزة.

### AUTH-05 — مرتفع — غياب حد تقني بين القناتين

كان `auth:sanctum` وحده يسمح من حيث المبدأ لأي Bearer Token صالح بالوصول
إلى أي مسار لا يضيف قيدًا آخر مناسبًا.

**المعالجة:** يفرض `EnsureAuthenticationChannel` نوع الحساب ونوع بيانات
الاعتماد والصلاحية الدقيقة:

- الويب: كائن `User` مع `TransientToken` الناتج عن جلسة Sanctum فقط.
- الموبايل: `Student` أو معلم، مع Personal Access Token ذي الغرض الصحيح.
- لا تُقبل صلاحية `*` بديلًا عن الصلاحية الدقيقة.

الدليل في `app/Http/Middleware/EnsureAuthenticationChannel.php:21-72`.
تبقى صلاحيات Spatie مطلوبة فوق هذا الحاجز؛ أي إن قناة المصادقة لا تلغي
التفويض الوظيفي.

### AUTH-06 — متوسط — ثقة الواجهة بحالة مخزنة محليًا

كانت الواجهة تحفظ الرمز وبيانات المستخدم في `localStorage`، ويمكن أن تصبح
حالة الواجهة منفصلة عن حقيقة الجلسة في الخادم.

**المعالجة:** لا تحفظ الواجهة أي سر مصادقة. يبقى الملف العام للمستخدم في
الذاكرة فقط، وعند تحميل لوحة التحكم يُسترجع من `/web/auth/me`
(`frontend/src/lib/stores/auth.svelte.js:26-68` و
`frontend/src/routes/(dashboard)/+layout.js:10-23`).

### AUTH-07 — متوسط — تسجيل خروج محلي فقط

كان حذف حالة الواجهة لا يضمن إبطال بيانات الاعتماد في الخادم.

**المعالجة:** تستدعي الواجهة `/web/auth/logout`، والخادم يسجل الخروج ويبطل
الجلسة ويجدد رمز CSRF
(`frontend/src/routes/(dashboard)/+layout.svelte:39-52` و
`app/Http/Controllers/WebAuthenticationController.php:59-69`).

## تدفق لوحة الويب

1. يطلب العميل `/sanctum/csrf-cookie`.
2. يرسل `POST /api/web/auth/login` مع `credentials: include` وترويسة
   `X-XSRF-TOKEN`.
3. يقبل الخادم الطلب فقط إذا كان مصدره ضمن
   `SANCTUM_STATEFUL_DOMAINS`، ثم يحدد المعدل بخمس محاولات في الدقيقة لكل
   بريد وIP.
4. بعد نجاح كلمة المرور يجدد الخادم معرّف الجلسة لمنع Session Fixation.
5. يعيد الخادم بيانات المستخدم العامة ووقت انتهاء تقريبيًا، من دون Token.
6. ترسل الطلبات اللاحقة Cookie الجلسة آليًا وتبقى خاضعة لـCSRF وصلاحيات
   Spatie.
7. عند `401` تمسح الواجهة حالتها وتعود إلى صفحة الدخول. وعند `419` تحصل
   على CSRF Cookie جديدة وتحاول الطلب مرة واحدة فقط.

تنفيذ CSRF و`credentials` موجود في
`frontend/src/lib/services/api.js:30-45,73-118`.

### Cookies

- `qims_web_session`: سر المصادقة؛ يجب أن يكون `HttpOnly=true`،
  و`Secure=true` في الإنتاج، و`SameSite=lax` افتراضيًا.
- `XSRF-TOKEN`: ليس سر مصادقة. من المقصود أن يكون قابلًا للقراءة من
  JavaScript كي يرسل العميل قيمته في `X-XSRF-TOKEN`.
- لا يقرأ كود الواجهة Cookie الجلسة ولا ينسخها إلى ترويسة Bearer.

توجد الإعدادات في `config/session.php:129-173` وبقية خصائص Cookie في القسم
نفسه، والقيم المحلية النموذجية في `.env.example`.

## تدفق تطبيق الموبايل

### المسارات

| الطلب | الغرض |
|---|---|
| `POST /api/mobile/auth/login` | إصدار زوج رموز جديد لجهاز طالب أو معلم |
| `GET /api/mobile/auth/me` | التحقق من Access Token واسترجاع الحساب |
| `POST /api/mobile/auth/refresh` | تدوير الزوج باستخدام Refresh Token |
| `POST /api/mobile/auth/logout` | إبطال زوج الجهاز الحالي |

استجابات إصدار الرموز تحمل `Cache-Control: no-store`، وتعيد وقت انتهاء كل
رمز. لا ترسل Cookies، ولا يقبل مسار التجديد Access Token بدل Refresh Token.

### قاعدة التخزين في عميل الموبايل

لا يوجد مشروع تطبيق موبايل داخل مساحة العمل الحالية، ولذلك ما تم تنفيذه هنا
هو عقد الخادم ومجموعة Bruno. عند دمج Flutter أو أي عميل أصلي يجب:

1. حفظ Access وRefresh Token في Keychain على iOS أو Keystore/تخزين مشفر
   مدعوم به على Android، لا في SharedPreferences عادية.
2. عدم تسجيل الرمز أو جسم استجابة الدخول في Logs أو Crash Reports أو
   Analytics.
3. إرسال Access Token فقط إلى مسارات البيانات، وRefresh Token فقط إلى مسار
   التجديد.
4. استبدال الرمزين ذريًا بعد نجاح التجديد، ومسح القديمين فورًا.
5. عند فشل التجديد بـ`401` تُمسح الرموز ويُطلب تسجيل الدخول مجددًا.
6. استخدام HTTPS فقط في الإنتاج. `http://localhost:8000` مخصص للاختبار
   المحلي وليس إعدادًا مقبولًا للإصدار.

## CORS وSanctum

- `CORS_ALLOWED_ORIGINS` يحتوي Origins كاملة مع البروتوكول والمنفذ، ولا
  يُستخدم `*` مع Cookies.
- `SANCTUM_STATEFUL_DOMAINS` يحتوي المضيف والمنفذ بلا بروتوكول.
- الطرق والترويسات المسموحة محصورة صراحة، و
  `supports_credentials=true` (`config/cors.php:18-52`).
- يجب أن تتشارك SPA والـAPI النطاق الأعلى نفسه عند استخدام Sanctum بهذه
  الصورة. مثال: `app.example.com` و`api.example.com`.
- عند استخدام نطاقين فرعيين اضبط `SESSION_DOMAIN=.example.com`. وإذا كانت
  المواقع Cross-Site فعلًا فـ`SameSite=None` يتطلب `Secure=true`، لكن
  الأفضل لهذا النظام إبقاء الواجهة والـAPI ضمن الموقع نفسه.

## انتهاء الصلاحية والتجديد

- جلسة الويب تنتهي بعد مدة الخمول في `SESSION_LIFETIME`، والطلبات النشطة
  تجدد عمر Cookie. لا يوجد Refresh Token للويب؛ فهذا غير مطلوب مع جلسة
  الطرف الأول.
- Access Token للموبايل قصير العمر وقابل للضبط عبر
  `MOBILE_ACCESS_TOKEN_MINUTES`.
- Refresh Token أطول عمرًا وقابل للضبط عبر
  `MOBILE_REFRESH_TOKEN_DAYS`.
- تدوير Refresh Token إجباري؛ الزوج السابق يصبح غير صالح فور نجاح
  التجديد.
- Logout الويب لا يحذف رموز أجهزة الموبايل. Logout الموبايل لا يلغي جلسة
  الويب أو أجهزة موبايل أخرى.

## الحد من المحاولات والتعداد

تعيد محاولات الدخول الفاشلة رسالة عامة لا تكشف هل الحساب موجود، ويطبق
الخادم الحدود التالية (`app/Providers/RouteServiceProvider.php:28-55`):

- خمسة طلبات دخول ويب في الدقيقة لكل بريد وIP.
- خمسة طلبات دخول موبايل في الدقيقة لكل اسم دخول وIP.
- عشرون طلب تجديد في الدقيقة لكل نوع حساب ومعرّف.
- 180 طلب API في الدقيقة لكل نوع حساب ومعرّف، لمنع تصادم رقم طالب مع رقم
  موظف.

## إعدادات الإنتاج الإلزامية

مثال توضيحي، مع استبدال النطاقات بالقيم الحقيقية:

```dotenv
APP_ENV=production
APP_DEBUG=false
APP_URL=https://api.example.com
FRONTEND_URL=https://app.example.com

SESSION_DRIVER=redis
SESSION_COOKIE=qims_web_session
SESSION_ENCRYPT=true
SESSION_SECURE_COOKIE=true
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=lax
SESSION_DOMAIN=.example.com

CORS_ALLOWED_ORIGINS=https://app.example.com
SANCTUM_STATEFUL_DOMAINS=app.example.com
SANCTUM_TOKEN_PREFIX=qims_
MOBILE_ACCESS_TOKEN_MINUTES=60
MOBILE_REFRESH_TOKEN_DAYS=30
```

بعد تعديل البيئة:

```bash
php artisan migrate
php artisan optimize:clear
php artisan config:cache
```

يجب أيضًا تشغيل Laravel Scheduler كل دقيقة كي تعمل مهمة تنظيف الرموز.
وعند تشغيل أكثر من نسخة من الخادم استخدم Redis أو مخزنًا مشتركًا للجلسات
ولـRate Limiting؛ لأن `SESSION_DRIVER=file` لا يشارك الجلسات بين العقد.

## أثر الانتقال

- المساران القديمان `/api/loginUser` و`/api/logout` أزيلا.
- الهجرة تبطل الرموز القديمة، لذلك يلزم تسجيل دخول جديد بعد النشر.
- لوحة الويب تستخدم المسارات الجديدة تلقائيًا.
- مجموعة Bruno للطالب تستخدم دخول الموبايل، وتحفظ زوج الرموز، وتختبر
  التجديد والإلغاء.
- لم تُعدّل أدوار Spatie أو صلاحيات بقية الأدوار في هذا التحديث؛ التغيير
  يضيف حاجز قناة فوق نظام الصلاحيات الموجود.

## التحقق المنفذ

- اختبارات Laravel كاملة: 64 اختبارًا و424 تحققًا.
- اختبارات المصادقة المتخصصة تغطي Cookie الآمنة، غياب Token من استجابة
  الويب، رفض المصادر غير الموثوقة، السماح للطالب والمعلم فقط في الموبايل،
  انتهاء الرموز، تدويرها، تسجيل الخروج لكل جهاز، منع عبور القنوات، وحذف
  الرموز القديمة.
- اختبارات عزل طالب الخدمة الذاتية تغطي اقتصار البيانات على الطالب
  المصادق عليه.
- `svelte-check`: صفر أخطاء وصفر تحذيرات.
- بناء SvelteKit الإنتاجي نجح.

## تحسينات تشغيلية لاحقة

ليست ثغرات مفتوحة في منطق المصادقة الحالي، لكنها مطلوبة قبل نشر متعدد
العقد أو تطبيق موبايل فعلي:

- تنفيذ التخزين الآمن والـrefresh interceptor داخل مشروع الموبايل عند
  توفره، ثم اختباره على iOS وAndroid.
- استخدام HTTPS وإضافة HSTS وسياسة CSP وترويسات الأمان عند Reverse Proxy.
- استخدام Redis مشترك للجلسات والـRate Limiting في النشر الأفقي.
- مراقبة محاولات الدخول والتجديد غير الطبيعية دون تسجيل كلمات المرور أو
  الرموز.
