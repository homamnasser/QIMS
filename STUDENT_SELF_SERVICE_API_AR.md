# توحيد وتأمين واجهات الطالب الذاتية

## الهدف

يقدّم هذا التحديث مساحة API موحّدة للطالب تحت المسار:

```text
/api/mobile/student/me/*
```

كلمة `me` هنا جزء من قاعدة الأمان: الخادم يستخرج الطالب من رمز Sanctum
المصادق عليه، ولا يقبل معرّف طالب من التطبيق لاختيار صاحب البيانات.

تغطي الواجهات بيانات الطالب الدراسية وسجلاته الشخصية:

| الواجهة | الصلاحية الخاصة بالطالب | نطاق البيانات |
|---|---|---|
| `GET /api/mobile/student/me/mosque` | `عرض مسجدي الدراسي` | مسجد الطالب الحالي |
| `GET /api/mobile/student/me/circles` | `عرض حلقاتي الدراسية` | حلقات الطالب المسجل فيها ضمن مسجده |
| `GET /api/mobile/student/me/courses` | `عرض كورساتي التعليمية` | الكورسات الناتجة عن تسجيلاته الفعلية |
| `GET /api/mobile/student/me/courses/{courseId}/schedule` | `عرض جداول كورساتي` | جدول كورس مسجل فيه الطالب ودروس كل يوم |
| `GET /api/mobile/student/me/notes` | `عرض ملاحظاتي كطالب` | الملاحظات الموجهة إلى الطالب |
| `GET /api/mobile/student/me/sabrs` | `عرض سبري كطالب` | سجلات السبر الخاصة بالطالب |
| `GET /api/mobile/student/me/memorizations` | `عرض تسميعاتي كطالب` | سجلات التسميع الخاصة بالطالب |
| `GET /api/mobile/student/me/warnings` | `عرض إنذاراتي كطالب` | الإنذارات الموجهة إلى الطالب |
| `GET /api/mobile/student/me/exams` | `عرض امتحاناتي كطالب` | نتائج امتحانات الطالب |
| `GET /api/mobile/student/me/reading-improvements` | `عرض تقييمات قراءتي كطالب` | تقييمات قراءة الطالب |
| `GET /api/mobile/student/me/final-results` | `عرض نتيجتي النهائية` | نتائج الطالب النهائية المنشورة فقط |
| `GET /api/mobile/student/me/final-results/{resultId}` | `عرض نتيجتي النهائية` | تفاصيل نتيجة منشورة تخص الطالب |
| `GET /api/mobile/student/me/certificates/{certificateId}` | `تحميل شهادتي النهائية` | ملف PDF لشهادة صادرة تخص الطالب |
| `POST /api/mobile/student/me/identity/confirm` | — | يفتح قسم النتيجة النهائية بعد تأكيد الهوية |
| `POST /api/mobile/student/me/identity/lock` | — | يقفل القسم فوراً عند مغادرته |

الواجهات الثلاث الأولى في هذا الجدول (`final-results` و`final-results/{id}`
و`certificates/{id}`) محروسة كلها بـ `student.identity.confirmed`.

## تأكيد الهوية يقفل قسم النتيجة النهائية

**منطق تسجيل الدخول لم يتغيّر:** الطالب يدخل باسم المستخدم، والموظف بالبريد
الإلكتروني. الرقم الذاتي ليس معرّف دخول ولا يصلح أن يكون كذلك، لأن الطالب غير
الملتحق بأي حلقة لا رقم ذاتي له بعد، فلو اعتمد الدخول عليه لتعذّر دخوله أصلاً.
الرقم الذاتي مقبول هنا وحده، لأن التأكيد يجري لحساب معروف مسبقاً من رمز الجلسة.

القفل على مستوى **القسم بأكمله** لا على مستوى تحميل الشهادة وحده: النتيجة
نفسها بيانات شخصية لا يجوز أن يقرأها من يمسك الجهاز بعد صاحبه، فلا معنى لحراسة
الشهادة وترك الدرجات مكشوفة. لذلك تُحرس القائمة والتفاصيل والشهادة معاً.

عند دخول القسم يرسل الطالب:

- `identifier`: اسم المستخدم **أو** رقمه الذاتي الحالي.
- `password`: كلمة مروره.

فإن صحّا مُنح إذن قصير العمر (**5 دقائق**) مرتبط برمز الوصول الحالي وحده، ويعيد
الخادم `data.expires_in` بالثواني. تأكيد على جهاز لا يفتح القسم على جهاز آخر
يحمل جلسة أخرى للطالب نفسه.

قبل التأكيد تعيد الواجهات الثلاث `403` برمز `IDENTITY_CONFIRMATION_REQUIRED`،
وهو رمز مستقل عن `403` الصلاحيات كي يميّزه التطبيق فيعرض شاشة التأكيد بدل رسالة
«غير مصرح» التي لا يملك الطالب حيالها فعلاً. وفشل التأكيد يعيد
`IDENTITY_CONFIRMATION_FAILED` **ويلغي أي إذن سابق**، فلا تترك محاولة خاطئة
القسم مفتوحاً بإذن قديم.

مهلة الخمس دقائق سقف احتياطي لا آلية القفل الأساسية: التطبيق يستدعي
`identity/lock` فور مغادرة القسم أو انتقاله إلى الخلفية، فيغلق فوراً. المهلة
تغطي الحالة التي لا يصل فيها إشعار الخروج، كإنهاء التطبيق فجأة.

تجري المقارنة مع أعمدة الطالب المصادق عليه نفسه، لا ببحث في جدول الطلاب:

- معرّف طالب آخر لا يفتح قسم هذا الحساب حتى لو كان صحيحاً في ذاته.
- الواجهة لا تصلح لتخمين معرّفات الطلاب، لأنها لا تستعلم عن أحد.
- ملكية البيانات تبقى مشتقّة من الرمز المصادق عليه كبقية واجهات `me`؛ التأكيد
  طبقة فوقها لا بديل عنها.

يُقصّ البياض الجانبي وتُوحَّد حالة الأحرف: اسم المستخدم مخزّن بأحرف صغيرة،
ورمز المسجد داخل الرقم الذاتي بأحرف كبيرة. لذلك `  eval01-000123  ` و
`EVAL01-000123` كلاهما مقبول من صاحبه.

تُفحص كلمة المرور دائماً ولو فشل المعرّف، حتى لا يكشف فرق زمن الاستجابة أي
الحقلين كان الخطأ. والواجهة محدودة بـ **خمس محاولات في الدقيقة لكل حساب**
(`throttle:identity-confirm`) لأنها تفحص كلمة مرور فهي سطح تخمين؛ والحدّ
بالحساب لا بالـ IP كي لا تُعطّل شبكة مسجد مشتركة طلابه جميعاً.

الطالب غير الملتحق بأي حلقة لا رقم ذاتي له، فيؤكد باسم المستخدم وحده ولا
يُحرم من نتيجته ولا من شهادته.

## مسار الشهادة والنتيجة النهائية

الترتيب مفروض من الخادم في كل خطوة، والتطبيق لا يملك تجاوزه:

1. تُعتمد النتيجة وتُنشر (`status = published`) من لوحة التحكم.
2. تُصدر الشهادة؛ ويرفض `CertificateService::issue` أي نتيجة غير منشورة.
3. تظهر النتيجة في `final-results` — الاستعلام مقيّد بـ `status = published`،
   والنتيجة المعتمدة غير المنشورة تعيد `404`.
4. تُحمَّل الشهادة من `certificates/{certificateId}`؛ ويشترط الخادم أن تكون
   `status = issued` وأن تعود لمرشح هذا الطالب، ويتحقق من بصمة `sha256` للملف
   قبل إرساله. الخطوتان 3 و4 معاً خلف تأكيد الهوية.

`certificateId` لا يخمّنه التطبيق: يأتي من `certificates[]` داخل استجابة
`final-results`، وهي محمّلة مسبقًا ومصفّاة على `status = issued`. النتيجة بلا
شهادة صادرة تعيد المصفوفة فارغة، فيخفي التطبيق زر التحميل.

## قيود المنطق السابق

كانت الواجهات القديمة تستخدم أسماء عامة مثل `getMyNotes` و`myExams`
وتشارك الصلاحيات نفسها بين الطالب والموظف. هذا جعل معنى كلمة «بي» يتغير حسب
نوع الحساب داخل الدالة نفسها، ونتجت عنه المشكلات التالية:

1. كانت واجهة الملاحظات الشخصية مبنية أساسًا لجلب الملاحظات التي كتبها
   الموظف عبر `notes.user_id`، لا الملاحظات الموجهة إلى الطالب عبر
   `notes.student_id`.
2. كانت واجهة الامتحانات تستخدم رقم الحساب المصادق عليه مباشرة كأنه رقم
   طالب، من دون التأكد من أن الحساب من نموذج `Student`.
3. حسابات الموظفين موجودة في جدول `users` وحسابات الطلاب في جدول
   `students`. لكل جدول تسلسل أرقام مستقل، ولذلك يمكن أن يوجد موظف وطالب
   يحملان الرقم نفسه. تساوي الرقمين لا يعني أنهما الشخص نفسه.
4. الصلاحيات القديمة مثل `عرض ملاحظاتي` و`امتحاناتي` لم توضّح هل المقصود
   سجل الموظف الذي أنشأه أم سجل الطالب الذي استلمه.
5. توزيع فروع الطالب والموظف بين Controllers وخدمات مختلفة يزيد احتمال
   نسيان شرط نوع الحساب أو استعمال عمود علاقة غير صحيح عند إضافة سجل جديد.
6. بعض الواجهات القديمة تقبل مرشحات مثل `student_id`. وجود هذا المدخل في
   واجهة ذاتية يوسّع سطح الخطأ؛ لأن ملكية البيانات يجب ألا تعتمد على قيمة
   يرسلها العميل.

## النهج المحدّث

يمر كل طلب ذاتي بالطبقات نفسها:

```text
رمز وصول موبايل قصير العمر بصلاحية mobile:access
    -> التحقق من الصلاحية الدقيقة
    -> التحقق أن الحساب كائن Student فعلي
    -> تمرير كائن الطالب إلى StudentLearningService
    -> الاستعلام عبر علاقة الطالب ومفتاحه الأجنبي
    -> إخراج البيانات من خلال API Resource
```

لا تستقبل واجهات السجلات الخمس أي `student_id`. حتى لو أضاف العميل هذا
المعامل إلى رابط الطلب فسيتم تجاهله، لأن الاستعلام يُبنى من كائن الطالب
المصادق عليه فقط:

- الملاحظات عبر علاقة `Student::notes()` وعمود `notes.student_id`.
- السبر عبر `Student::sabrs()` وعمود `sabrs.student`.
- التسميع عبر `Student::memorizations()` وعمود `memorizations.student`.
- الإنذارات عبر `Student::warnings()` وعمود `warnings.student`.
- الامتحانات عبر `Student::exams()` وعمود `exams.student`.

كما يتحقق Controller صراحة من أن الحساب المصادق عليه `instanceof Student`.
لذلك لا يستطيع حساب موظف استدعاء واجهات `/mobile/student/me/*` حتى لو
أُسندت إليه صلاحية طالب بالخطأ. كما لا تقبل هذه الواجهات جلسة لوحة التحكم
ولا رمزًا قديمًا عامًا بصلاحية `*`.

## فصل صلاحيات الطالب عن صلاحيات الموظفين

لم تُحذف الصلاحيات القديمة لأنها مستخدمة في واجهات الموظفين الحالية، ولم
تُسحب من أدوار المعلمين أو المشرفين أو المديرين. بدلًا من ذلك أضيفت خمس
صلاحيات صريحة تنتهي بعبارة «كطالب».

تقوم الهجرة الجديدة بما يلي:

1. تنشئ صلاحيات سجلات الطالب الجديدة إن لم تكن موجودة.
2. تمنحها فقط للأدوار ذات `role_family = student`.
3. تسحب الصلاحيات الخمس القديمة الملتبسة من أدوار عائلة الطالب فقط.
4. تبقي صلاحيات كل العائلات الأخرى دون تعديل.
5. تمسح ذاكرة Spatie المؤقتة قبل العملية وبعدها لتطبيق النتائج مباشرة.

يبقى Spatie هو مصدر قرار السماح، بينما تحدد `role_family` مجموعة الصلاحيات
الإلزامية لنوع الحساب. اسم الدور المعروض لا يُستخدم وحده لمنح الوصول.

## لماذا النهج الجديد أكثر أمانًا؟

- **ملكية مشتقة من المصادقة:** لا يستطيع العميل تبديل رقم الطالب في الرابط
  أو معاملات الاستعلام.
- **دفاع بطبقتين:** يلزم امتلاك الصلاحية ويلزم أيضًا أن يكون الحساب طالبًا
  فعليًا.
- **منع تداخل المعرّفات:** لا تتم مقارنة رقم من جدول `users` بحقل يشير إلى
  جدول `students`.
- **أقل قدر من الصلاحيات:** يحصل الطالب على صلاحيات القراءة الذاتية فقط، ولا
  يحصل على صلاحيات عرض القوائم العامة.
- **فشل آمن لجدول الكورس:** الكورس غير الموجود والكورس الذي لا يخص الطالب
  يعيدان استجابة `404` نفسها، فلا تتحول الواجهة إلى وسيلة لاستكشاف الكورسات.
- **اختبارات عزل مباشرة:** تتضمن الاختبارات طالبين وموظفين بأرقام متطابقة،
  وتثبت أن كل واجهة تعيد سجل الطالب المصادق عليه وحده.

## لماذا النهج أكثر قابلية للتوسع؟

- جميع واجهات الطالب موجودة تحت Prefix واحد وبنمط تسمية متوقع.
- إضافة نوع سجل جديد تتطلب صلاحية طالب صريحة، ودالة خدمة تستقبل
  `Student`، ومسارًا داخل المجموعة نفسها.
- الاستعلامات عبر علاقات نموذج الطالب تقلل تكرار أسماء الأعمدة وتقلل فرص
  اختيار علاقة الموظف بدل علاقة الطالب.
- فصل صلاحيات الطالب عن أسماء صلاحيات الموظفين يسمح بتطوير تجربتي الطالب
  والموظف باستقلال من دون تغيير معنى صلاحية مستخدمة مسبقًا.
- Resources تفصل شكل الاستجابة عن الاستعلام، مما يسهّل تطوير تطبيق جوال أو
  واجهة ويب للطالب من دون فتح استعلامات إدارية عامة.

## لماذا هذا النمط أنسب لبيانات الطالب؟

الملاحظات والإنذارات والنتائج والتسميعات سجلات شخصية، وليس المطلوب منها
بحثًا إداريًا عامًا. لذلك يجب أن يكون السؤال الذي تجيب عنه الواجهة دائمًا:
«ما السجلات المرتبطة بهذا الطالب المصادق عليه؟» لا «ما الطالب الذي طلب
العميل رقمه؟».

هذا الفرق يجعل حدود الوصول جزءًا ثابتًا من تصميم الواجهة والاستعلام، بدل أن
تكون شرطًا اختياريًا يعتمد على انضباط التطبيق المستهلك.

## التشغيل والاختبار المحلي

بعد سحب التغييرات شغّل:

```bash
php artisan migrate
php artisan permission:cache-reset
php artisan test
```

توجد مجموعة Bruno في:

```text
api/Student Self Service API/
```

اختر بيئة `Local` التي تستخدم `http://localhost:8000`، ثم نفّذ طلب تسجيل
الدخول أولًا. تحفظ المجموعة رمز الوصول القصير في `studentToken` ورمز
التجديد الدوّار في `refreshToken` تلقائيًا. بعدها يمكن تشغيل طلبات المسجد
والحلقات والكورسات والجدول والسجلات الشخصية، وتجديد الزوج من الطلب رقم 12،
ثم إلغاء زوج رموز هذا الجهاز من الطلب رقم 13.

في تطبيق الموبايل الحقيقي يجب حفظ الرمزين في مخزن الأسرار الآمن الذي يوفره
نظام التشغيل، وعدم وضعهما في التخزين العادي أو السجلات. أما لوحة التحكم فلا
تستقبل هذين الرمزين أصلًا؛ فهي تستخدم جلسة Cookie منفصلة محمية بـ
`HttpOnly` وCSRF.
