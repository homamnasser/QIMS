info:
  name: PUT Update Absence
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/api/mobile/staff/attendance/:id"
  params:
    - name: id
      value: "{{attendanceId}}"
      type: path
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  auth:
    type: bearer
    token: "{{staffToken}}"
  body:
    type: json
    data: |-
      {
        "date": "2026-08-01",
        "type": "full",
        "note": "تحديث سجل الحضور من تطبيق الموبايل"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("passes mobile staff authentication and authorization", function() {
          expect(res.status).to.not.equal(401);
          expect(res.status).to.not.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  يختبر `PUT api/mobile/staff/attendance/{id}` باستخدام Bearer Access Token خاص بقناة كادر الموبايل.
  
  الصلاحية المطلوبة: `تعديل الغياب`.
  
  تنبيه: قد يغيّر هذا الطلب البيانات المحلية.
