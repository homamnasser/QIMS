info:
  name: PUT Update Exam
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/api/mobile/staff/exams/:id"
  params:
    - name: id
      value: "{{examId}}"
      type: path
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  auth:
    type: bearer
    token: "{{staffToken}}"
  body:
    type: json
    data: |-
      {
        "mark": 95
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("passes mobile staff authentication and authorization", function() {
          expect(res.status).to.not.equal(401);
          expect(res.status).to.not.equal(403);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  يختبر `PUT api/mobile/staff/exams/{id}` باستخدام Bearer Access Token خاص بقناة كادر الموبايل.
  
  الصلاحية المطلوبة: `تعديل الامتحان`.
  
  تنبيه: قد يغيّر هذا الطلب البيانات المحلية.
