info:
  name: GET Readiness
  type: http
  seq: 11
  tags:
    - web-session
    - read

http:
  method: GET
  url: "{{baseUrl}}/api/evaluation-cycles/:cycle/readiness"
  params:
    - name: cycle
      value: "{{evaluationCycleId}}"
      type: path
  headers:
    - name: Accept
      value: "application/json"
    - name: Origin
      value: "{{webOrigin}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("passes web authentication, channel and CSRF checks", function() {
          expect(res.status).to.not.equal(401);
          expect(res.status).to.not.equal(403);
          expect(res.status).to.not.equal(419);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  يختبر `GET /api/evaluation-cycles/{cycle}/readiness` ضمن مجموعة Evaluations.

  المسار يستخدم جلسة الويب HttpOnly الخاصة بـSanctum، ولا يقبل Bearer Token للموبايل.

  الصلاحية المطلوبة: `عرض تقييمات الطلاب النهائية`.
