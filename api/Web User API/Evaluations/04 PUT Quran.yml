info:
  name: PUT Quran
  type: http
  seq: 4
  tags:
    - web-session
    - write

http:
  method: PUT
  url: "{{baseUrl}}/api/evaluation-candidates/:candidate/quran-assessment"
  params:
    - name: candidate
      value: "{{evaluationCandidateId}}"
      type: path
  headers:
    - name: Accept
      value: "application/json"
    - name: Origin
      value: "{{webOrigin}}"
    - name: X-XSRF-TOKEN
      value: "{{xsrfToken}}"
  body:
    type: json
    data: |-
      {
        "evaluation_period_id": "{{evaluationPeriodId}}",
        "circle_id": "{{circleId}}",
        "below_minimum": false,
        "notes": "تحقق تجريبي من الحد الأدنى"
      }


runtime:
  scripts:
    - type: tests
      code: |-
        test("passes web authentication, channel and CSRF checks", function() {
          expect(res.status).to.not.equal(401);
          expect(res.status).to.not.equal(403);
          expect(res.status).to.not.equal(419);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  يختبر `PUT /api/evaluation-candidates/{candidate}/quran-assessment` ضمن مجموعة Evaluations.

  المسار يستخدم جلسة الويب HttpOnly الخاصة بـSanctum، ولا يقبل Bearer Token للموبايل.

  الصلاحية المطلوبة: `إدخال تقييم القرآن`.

  تنبيه: هذا الطلب قد يغيّر البيانات. راجع القيم قبل الإرسال.
