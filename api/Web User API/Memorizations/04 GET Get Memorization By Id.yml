info:
  name: GET Get Memorization By Id
  type: http
  seq: 4
  tags:
    - web-session
    - read

http:
  method: GET
  url: "{{baseUrl}}/api/memorization/getMemorizationById/:id"
  params:
    - name: id
      value: "{{memorizationId}}"
      type: path
  headers:
    - name: Accept
      value: "application/json"
    - name: Origin
      value: "{{webOrigin}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("passes web authentication, channel and CSRF checks", function() {
          expect(res.status).to.not.equal(401);
          expect(res.status).to.not.equal(403);
          expect(res.status).to.not.equal(419);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  يختبر `GET /api/memorization/getMemorizationById/{id}` ضمن مجموعة Memorizations.

  المسار يستخدم جلسة الويب HttpOnly الخاصة بـSanctum، ولا يقبل Bearer Token للموبايل.

  الصلاحية المطلوبة: `عرض تسميع الطالب`.
