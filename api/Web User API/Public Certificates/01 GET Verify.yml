info:
  name: GET Verify
  type: http
  seq: 1
  tags:
    - public
    - read

http:
  method: GET
  url: "{{baseUrl}}/api/public/certificates/verify/:token"
  params:
    - name: token
      value: "{{certificateVerificationToken}}"
      type: path
  headers:
    - name: Accept
      value: "application/json"
    - name: Origin
      value: "{{webOrigin}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns a controlled public API response", function() {
          expect([200, 201, 404, 409, 422]).to.include(res.status);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  يختبر `GET /api/public/certificates/verify/{token}` ضمن مجموعة Public Certificates.

  المسار عام ولا يعتمد على جلسة الموظف، لكنه يبقى خاضعًا للتحقق من المدخلات وتحديد المعدل.
