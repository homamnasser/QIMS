# نظام الاستبيانات

## نظرة عامة

أضيف نظام استبيانات كامل إلى QIMS مع الحفاظ على نمط Laravel API وSpatie
Permissions وواجهة Svelte العربية. يفصل النظام بين تعريف الاستبيان والحقول
والأسئلة والقواعد والردود والملفات، ويحفظ لقطة تاريخية عند كل إرسال.

## الصلاحيات

تُنشأ الصلاحيات تلقائيًا عبر migration وتوجد أيضًا في `PermissionSeeder`:

- إنشاء استبيان.
- تعديل استبيان.
- حذف استبيان.
- عرض تفاصيل الاستبيان.
- عرض كافة الاستبيانات.
- نشر وإلغاء نشر الاستبيان.
- عرض وتصدير ردود الاستبيان.

يجب إسناد الصلاحيات المطلوبة للأدوار من صفحة الأدوار والصلاحيات. المستخدم
`super-admin` يتجاوز بوابات الصلاحيات وفق آلية المشروع الحالية.

## الرقم الذاتي للطالب

- يتطلب كل مسجد رمزًا فريدًا بالشكل `O` أو `ANSAR1`.
- عند أول إلحاق ناجح للطالب بحلقة، يصل النظام إلى المسجد عبر
  `circle -> course -> mosque`.
- يقفل النظام سجل الطالب وسجل المسجد داخل transaction، يزيد
  `next_student_sequence`، ثم ينشئ رقمًا مثل `O-000001`.
- يبقى الرقم فعالًا ما دام الطالب ملتحقًا بحلقة واحدة على الأقل. عند إزالة
  آخر إلحاق، يُمسح `selfnumber` و`mosque_id` من سجل الطالب الفعال ويُعلّم
  الرقم كموقوف في `student_selfnumber_reservations`.
- يحتفظ سجل الحجوزات بكل رقم سبق إصداره بقيد `unique` دائم، لذلك لا يمكن
  إعادة استخدام رقم موقوف. تستمر قيمة `next_student_sequence` بالزيادة داخل
  المسجد، ويحصل أي إلحاق لاحق على الرقم التالي حتى عند حذف إلحاقات سابقة.
- يستخدم `TestDataSeeder` خدمة الإلحاق نفسها المستخدمة في التطبيق، لذلك يحصل
  الطلاب التجريبيون الملتحقون بحلقات في مساجد ذات رمز صالح على أرقامهم تلقائيًا.
- قيد `unique` على `students.selfnumber` وسجل الحجوزات وقيد فريد على إلحاق
  `student + circle` تشكل خطوط دفاع إضافية ضد التكرار.
- لا يتغير الرقم الذاتي القديم إذا عُدّل رمز المسجد لاحقًا، لأنه معرّف تاريخي
  دائم للطالب.

يمكن ضبط طول الجزء الرقمي من:

```dotenv
SURVEY_SELFNUMBER_SEQUENCE_LENGTH=6
```

## توقيت مواعيد الاستبيان

- يعرض حقل `datetime-local` الوقت وفق توقيت جهاز الموظف، ثم تحوله الواجهة إلى
  UTC صريح قبل إرساله إلى الخادم.
- إذا استقبل الخادم قيمة قديمة بلا فرق توقيت، يفسرها وفق
  `SURVEY_SCHEDULE_TIMEZONE` (القيمة الافتراضية `Asia/Damascus`) ثم يخزنها UTC.
- ترحيل `000400_normalize_survey_schedule_timezone` يصحح المواعيد الموجودة التي
  أُدخلت قبل هذا الإصلاح كتوقيت دمشق وخُزنت خطأ كأنها UTC.

```dotenv
SURVEY_SCHEDULE_TIMEZONE=Asia/Damascus
```

## دورة الاستبيان

1. ينشئ الموظف مسودة مع الاسم والوصف والصورة والمدة وإعداد تكرار الرد.
2. يختار حقول الطالب المسموح عرضها ويضيف الأقسام والأسئلة والخيارات.
3. يضيف قواعد `show_if` أو `hide_if` أو `require_if`.
4. قبل النشر، يتحقق الخادم من الصورة والتواريخ والخيارات والمراجع وترتيب
   الاعتمادات والحلقات الدائرية.
5. يستخدم الطالب الرابط العام، ويدخل `selfnumber`.
6. يصدر الخادم جلسة مشفرة محدودة المدة مرتبطة بالطالب والاستبيان.
7. عند الإرسال يعيد الخادم التحقق من الإتاحة والأهلية والمنطق والحقول والملفات،
   ثم يحفظ الرد واللقطات التاريخية.

حقول الطالب المتاحة معرفة في allow-list داخل
`SurveyStudentFieldRegistry`. الحقول الحساسة لا تظهر للمنشئ ما لم يملك صلاحية
`عرض تفاصيل الطالب`. الملفات المرفوعة تحفظ على القرص الخاص `local` ولا يمكن
تنزيلها إلا عبر endpoint محمي بصلاحية عرض الردود.

## أهم واجهات API

المسارات الإدارية كلها تحت `/api/surveys` ومحميّة بـ Sanctum والصلاحية المناسبة:

- `GET /surveys`
- `POST /surveys`
- `GET|PUT|DELETE /surveys/{id}`
- `PUT /surveys/{id}/definition`
- `POST /surveys/{id}/publication`
- `GET /surveys/{id}/responses`
- `GET /surveys/{id}/responses/{response}`
- `GET /surveys/files/{accessToken}`

المسارات العامة تستخدم رمزًا عشوائيًا لا يكشف رقم قاعدة البيانات:

- `GET /public/surveys/{publicToken}`
- `POST /public/surveys/{publicToken}/identify`
- `POST /public/surveys/{publicToken}/responses`

## التقارير

صفحة الردود تدعم البحث، تاريخ البداية والنهاية، الترتيب، عرض تفاصيل الرد،
تنزيل المرفقات، تصدير جميع الصفحات إلى Excel، وإنشاء تقرير عربي RTL عبر
نافذة الطباعة التي يمكن حفظها كملف PDF.

## التشغيل بعد الدمج

من مجلد `QIMS`:

```bash
php artisan migrate
php artisan storage:link
php artisan optimize:clear
php artisan test
```

تأكد من ضبط عنوان الواجهة لتوليد الروابط الصحيحة:

```dotenv
FRONTEND_URL=https://frontend.example.com
SURVEY_STUDENT_TOKEN_MINUTES=30
```

ومن مجلد `frontend`:

```bash
npm run check
npm run build
```

لا تستخدم `TestDataSeeder` في الإنتاج؛ migration الخاصة بالاستبيانات تنشئ
الصلاحيات المطلوبة دون تشغيل بيانات الاختبار.
